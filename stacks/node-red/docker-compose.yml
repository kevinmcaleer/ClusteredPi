version: '3'
services:
  node-red:
    container_name: nodered/node-red
    image: "nodered"
    volumes:
      - /home/pi/nodered:/data
    restart: unless-stopped
    privileged: true

    # networks:
    #   mymacvlanActive:
    #     ipv4_address: 192.168.1.150
       
networks:
  mymacvlanActive:
    external: true
    
    
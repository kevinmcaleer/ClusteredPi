FROM nginx

# Copy nginx config from build context (local file, not GitHub)
COPY nginx.conf /etc/nginx/nginx.conf

# Note: Removed git clone - we build from local files now
# This ensures the latest config changes are included
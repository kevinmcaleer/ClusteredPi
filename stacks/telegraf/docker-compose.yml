services:
  telegraf:
    image: telegraf
    volumes:
      - ./telegraf.conf:/etc/telegraf/telegraf.conf
    restart: always
    networks:
      - monitoring_net
    hostname: {{.Node.Hostname}}
networks:
  monitoring_net:
    driver: bridge
  
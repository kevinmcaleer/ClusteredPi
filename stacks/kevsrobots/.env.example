# Cloudflare Tunnel token — same token on every node.
# All nodes register as connectors for the same tunnel;
# Cloudflare load-balances across them automatically.
TUNNEL_TOKEN=your-tunnel-token-here

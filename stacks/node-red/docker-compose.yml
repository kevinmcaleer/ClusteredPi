services:
  nodered:
    ports: 
      - "1881:1880"
      - "444:443"
      - "8081:80"
    container_name: nodered
    image: "nodered/node-red"
    volumes:
      - /home/kev/nodered:/data
    restart: unless-stopped
    privileged: true

    # networks:
    #   mymacvlanActive:
    #     ipv4_address: 192.168.1.150
       
# networks:
#   mymacvlanActive:
#     external: true
    
    
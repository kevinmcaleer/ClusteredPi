# Cloudflare Tunnel token for the chatter tunnel.
TUNNEL_TOKEN=your-tunnel-token-here

# Node name for dashboard identification.
NODE_NAME=dev01

# PostgreSQL connection string.
DATABASE_URL=postgresql://kev:mysecretpassword@192.168.2.3:5433/pagecount

# Chatter app secret key.
SECRET_KEY=changeme

# Environment (production/development).
ENVIRONMENT=production
PORT=8006

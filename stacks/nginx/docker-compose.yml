version: "3.9"
services:
  myapp_load_balancer:
    build: .
    ports: 
      - "80:80"
      - "8080:8080"
      - "443:443"
    # image: myapp
    restart: always
    volumes:
      - ./certbot/conf:/etc/letsencrypt:ro
      - ./certbot/data:/var/www/certbot:ro
  certbot:
    image: certbot/certbot:arm32v6-latest
    container_name: certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt:rw
      - ./certbot/data:/var/www/certbot:rw
    command: certonly --webroot-path=/var/www/certbot --force-renewal --email kevinmcaleer@gmail.com -d clustered-pi.com --agree-tos 
# Cloudflare Tunnel token for the storybook tunnel.
# Create a tunnel in the Cloudflare Zero Trust dashboard (Networks > Tunnels),
# add a public hostname storybook.kevsrobots.com -> http://localhost:6006,
# then copy the tunnel token here.
TUNNEL_TOKEN=your-tunnel-token-here
